# ScanPang Backend 환경 변수
# Render.com 배포 시 DATABASE_URL이 자동 설정됨

# === DB 연결 (택 1) ===
# 방법 1: DATABASE_URL (Render 자동 제공)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# 방법 2: 개별 파라미터 (로컬/Supabase)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=scanpang_mvp
DB_USER=scanpang
DB_PASSWORD=password

# === 서버 설정 ===
PORT=3000
NODE_ENV=development

# === 인증 ===
API_KEY=your-api-key-here

# === CORS ===
CORS_ORIGIN=*
